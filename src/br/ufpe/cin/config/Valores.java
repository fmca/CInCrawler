package br.ufpe.cin.config;

/**
 * Valores e constantes compartilhadas por toda a aplicação
 * 
 * @author Filipe (fmca)
 * */
public enum Valores {

	excel, saida, urlPaginaProfessores, maxNumPagina, minNumPagina, numCrawlers, parametroVisitarPagina, nomeArquivoSaida
}
